title: "Start Screenrecord Android"
summary: A short summary of the step. Don't make it too long ;)
description: A short summary of the step. Don't make it too long ;)
website: https://github.com/bitrise-steplib/steps-start-screenrecord-android
source_code_url: https://github.com/bitrise-steplib/steps-start-screenrecord-android
support_url: https://github.com/bitrise-steplib/steps-start-screenrecord-android/issues
host_os_tags:
  - ubuntu
type_tags:
  - utility
is_requires_admin_user: true
is_always_run: false
is_skippable: false
deps:
  brew:
  - name: go
  apt_get:
  - name: golang
    bin_name: go
toolkit:
  go:
    package_name: github.com/bitrise-steplib/steps-start-screenrecord-android
run_if: ""
inputs:
  - id: "record0"
    opts:
      title: "ID"
      summary: Make sure this input's value is unique. This ID will be included in the recorded file's name to be able to separate records, and also `Stop Screenrecord Android` step will need this ID to know which record need to be stopped.
      description: Make sure this input's value is unique. This ID will be included in the recorded file's name to be able to separate records, and also `Stop Screenrecord Android` step will need this ID to know which record need to be stopped.
      is_expand: true
      is_required: true
  - emulator_serial: $BITRISE_EMULATOR_SERIAL
    opts:
      title: "Emulator serial"
      summary: The commands will be sent to the emulator that has the given serial.
      description: The commands will be sent to the emulator that has the given serial.
      is_expand: true
      is_required: true
